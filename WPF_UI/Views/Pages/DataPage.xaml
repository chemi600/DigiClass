<Page
    x:Class="Wpf.Ui.Demo.Mvvm.Views.Pages.DataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.Ui.Demo.Mvvm.Views.Pages"
    xmlns:viewmodel="clr-namespace:Wpf.Ui.Demo.Mvvm.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Wpf.Ui.Demo.Mvvm.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DataPage"
    d:DataContext="{d:DesignInstance viewmodel:DataViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Grid Margin="42">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" Margin="5">
        <ui:Button Content="Recargar"  VerticalAlignment="Top" Command="{Binding RecargarCommand,Mode=OneWay}"/>
        <ui:Button Content="Add"  VerticalAlignment="Top" Command="{Binding GoToPageCommand,Mode=OneWay}"/>
        </StackPanel>
        <DataGrid 
    Grid.Row="1"
     Grid.ColumnSpan="2"       
    x:Name="MyDataGrid"       
    ItemsSource="{Binding Characters,Mode=TwoWay}" 
    AutoGenerateColumns="False" 
    Margin="10" 
    CanUserResizeColumns="True"
    CanUserResizeRows="False"
    CanUserAddRows="False"
    VerticalAlignment="Top"
    HorizontalAlignment="Left"
    MinRowHeight="25"
    MaxHeight="400" 
    ColumnWidth="Auto"
    ScrollViewer.VerticalScrollBarVisibility="Auto"
    ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding id}" />
                <DataGridTextColumn Header="Titulo" Binding="{Binding titulo}" />
                <DataGridTextColumn Header="Descripcion" Binding="{Binding descripcion}"/>
                <DataGridTextColumn Header="Prodesor" Binding="{Binding nombreProfesor}"/>
                <DataGridTextColumn Header="Fecha de Inicio" Binding="{Binding fechaInicio}"/>
                <DataGridTextColumn Header="Fecha de fin" Binding="{Binding fechaFin}"/>

                <DataGridTemplateColumn Header="Acciones">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Eliminar" Command="{Binding DataContext.Eliminar_Click,RelativeSource={RelativeSource AncestorType=DataGrid}}}"  />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10" Grid.Row="2">
            <Button Content="Previous" Command="{Binding PreviousCommand}"/>
            <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="5"/>
            <Button Content="Next" Command="{Binding NextCommand}"/>
        </StackPanel>
    

    </Grid>
</Page>
