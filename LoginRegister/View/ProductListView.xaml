<UserControl x:Class="InfoManager.View.ProductListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:InfoManager.View"
        Background="#DDA853"
             xmlns:viewmodels="clr-namespace:InfoManager.ViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:ProductListViewModel}"
        Height="auto" Width="auto">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="70"/>
        </Grid.ColumnDefinitions>

        <Label Content="Filtro" HorizontalAlignment="Center" Grid.Row="0" Grid.Column="1"/>
        <ui:TextBox PlaceholderText="Filtro" Width="200" Height="25" Grid.Row="1" Grid.Column="1" Text="{Binding Filtro,Mode=TwoWay}"/>
        <ui:Button Content="Buscar" Grid.Row="1" Grid.Column="2" Width="40" Command="{Binding FilterCommand}"/>
        <ui:Button Grid.Row="1" Width="60" Grid.Column="0" HorizontalAlignment="Right" Content="Recargar" Visibility="{Binding MostrarButtonRecarga}"/>
        <ui:Button Grid.Row="1" Width="60" Grid.Column="0" HorizontalAlignment="Right" Content="Cerrar" Command="{Binding CerrarCommand}" Visibility="{Binding MostrarButtonCerrar}"/>
        <ScrollViewer Grid.Row="2" Grid.ColumnSpan="3"  >
            <DataGrid 

    x:Name="MyDataGrid"       
    ItemsSource="{Binding Items,Mode=TwoWay}" 
    AutoGenerateColumns="False" 
    Margin="10" 
    CanUserResizeColumns="True"
    CanUserResizeRows="False"
    CanUserAddRows="False"
    VerticalAlignment="Top"
    HorizontalAlignment="Center"
    MinRowHeight="25"
    MaxHeight="400" 
    ColumnWidth="Auto"
    ScrollViewer.VerticalScrollBarVisibility="Auto"
    ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding id}" />
                    <DataGridTextColumn Header="Titulo" Binding="{Binding titulo}" />
                    <DataGridTextColumn Header="Descripcion" Binding="{Binding descripcion}"/>
                    <DataGridTextColumn Header="Profesor" Binding="{Binding nombreProfesor}"/>
                    <DataGridTextColumn Header="Fecha de Inicio" Binding="{Binding fechaInicio}"/>
                    <DataGridTextColumn Header="Fecha de fin" Binding="{Binding fechaFin}"/>
                    
                    <DataGridTemplateColumn Header="Ver">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Ver" Command="{Binding DataContext.MostrarPanelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Acciones">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Eliminar" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>

        <Border Grid.Column="1" Grid.ColumnSpan="2" Grid.RowSpan="4" Background="#EEE"  Panel.ZIndex="10"
            Visibility="{Binding Mostrar}">
            <ContentControl Content="{Binding SelectedViewModel}"/>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10" Grid.Row="3" Grid.Column="1">
            <Button Content="Previous" Command="{Binding PreviousCommand}"/>
            <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="5"/>
            <Button Content="Next" Command="{Binding NextCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
